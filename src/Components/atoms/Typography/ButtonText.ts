import styled from 'styled-components';
import tw from 'twin.macro';

export default styled.p`
	font-weight: 500;
	${tw`
      text-inherit

      text-sm
      leading-5

      sm:text-base
      sm:leading-6
  `}
`;
